<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="faux_shell_hamburger.MainPage">

    <ScrollView>
        <Grid
            RowDefinitions="50,*">
            <Button
                Text="☰"
                FontSize="Medium"
                CornerRadius="0"
                HorizontalOptions="Start"
                WidthRequest="50"
                TextColor="{StaticResource Primary}"
                BackgroundColor="{StaticResource Secondary}"
                Clicked="ShowFlyout"/>
        <VerticalStackLayout
            Grid.Row="1"
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
            </VerticalStackLayout>
            <Frame
                Grid.Row="1"
                BackgroundColor="DarkGray"
                Opacity="0.8">
            </Frame>
            <VerticalStackLayout
                WidthRequest="250"
                HorizontalOptions="Start"
                Spacing="2"
                Grid.Row="1"
                BackgroundColor="WhiteSmoke">
                <HorizontalStackLayout HorizontalOptions="Start">
                    <Button Text="✈" FontSize="Medium" WidthRequest="50"/>
                    <Label Text="Flights" VerticalTextAlignment="Center" Padding="5,0" FontSize="Medium" />
                </HorizontalStackLayout>
                <HorizontalStackLayout HorizontalOptions="Start">
                    <Button Text="🌞" FontSize="Medium" WidthRequest="50" />
                    <Label Text="Beach Activities" VerticalTextAlignment="Center" Padding="5,0" FontSize="Medium" />
                </HorizontalStackLayout>
                <HorizontalStackLayout HorizontalOptions="Start">
                    <Button Text="✉" FontSize="Medium" WidthRequest="50" />
                    <Label Text="Mail" VerticalTextAlignment="Center" Padding="5,0" FontSize="Medium"/>
                </HorizontalStackLayout>
                <HorizontalStackLayout HorizontalOptions="Start">
                    <Button Text="📸" FontSize="Medium" WidthRequest="50" />
                    <Label Text="Photos" VerticalTextAlignment="Center" Padding="5,0" FontSize="Medium" />
                </HorizontalStackLayout>
                <HorizontalStackLayout HorizontalOptions="Start">
                    <Button Text="🏆" FontSize="Medium" WidthRequest="50" />
                    <Label Text="Awards" VerticalTextAlignment="Center" Padding="5,0" FontSize="Medium" />
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </Grid>
    </ScrollView>

</ContentPage>
